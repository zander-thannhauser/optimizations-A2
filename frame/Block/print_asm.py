
def Block_print_asm(this, already):
	assert(not "TODO");
#	if this not in already:
#		already.add(this);
#		if (this.label):
#			printf("%s: ", this.label);
#		print("%u instructions...", len(this.instructions));
#		for c in this.children:
#			c.print_asm(c, already);

